-Pages user qu'il me reste à créer( types de gestionnaires [ COMMERCIAL, RESPONSABLE_VENTE_CREDIT, RESPONSABLE_ECONOMIQUE, RESPONSABLE_MARKETING ] )

-Enlève moi la partie inscription ( auth/login ) ou plut^^ot bloque là pour empêcher n'importe qui de s'inscire sur l'application (espion potentiel)  

-Je viens de découvrir Print CSS et window.print() pour les export PDF/Excel
On peut aussi utiliser du SVG pour les graphiques ( sans forcément dépendance externe)  
Ce code :  
 const a = document.createElement("a");
 a.href = url;
 a.download = filename;
 a.click();   
 permet de créer un téléchargement via un clic sur une URL ( utile pour les exports)

Claude , en fait on doit revoir la base de donnée à partir de zéro (j'avais pas bien compris toute la structure de données de l'application ). Bon en fait , les USER doivent quasiment tous être rattaché à des points de vente et donc à des RPV(responsablepoint de vente), ainsi que les clients et les vendeurs( bon pour vendeur , ça peut être les user agent_terrain , les magasiniers, agent_logistique ou même le RPV et l'admin). Aussi on doit pouvoir gérer les ventes directes (pas juste les ventes liées à des packs)
  

    