-Pages user qu'il me reste à créer( types de gestionnaires [ COMMERCIAL, RESPONSABLE_VENTE_CREDIT, RESPONSABLE_ECONOMIQUE, RESPONSABLE_MARKETING ] )

-exceljs est une lib pour styliser les exports

-"test": "vitest run",
 11 +    "test:watch": "vitest",


-Chromium headless est une lib qui permet le rendu pdf

-Print CSS et window.print() pour les export PDF/Excel

-Resend est une lib permettant l'envoi d'email 
   
-jose pour le JWT, bcryptjs pour le hash
    
-une petite dépendance (qrcode / bwip-js) pour QR scannable (et un vrai code-barres Code128) 

-Ecrire toujours un script npm migrate:deploy:prod pour migrer via l'endpoint direct (host sans -pooler) pour éviter d'utiliser le pooler Neon et empêcher les locks
  

-On peut aussi utiliser du SVG pour les graphiques ( sans forcément dépendance externe)  
-Ce code :      
 const a = document.createElement("a");
 a.href = url;                          
 a.download = filename;                                                
 a.click();                                                                     
 permet de créer un téléchargement via un clic sur une URL ( utile pour les exports)

-import { randomUUID } from "crypto";  permet de générer des UUID(sorte d'identifiant unique aléatoire) par exemple pour des factures ou des pièces justificatives
-Prisma 7 est une breaking change majeure : url dans schema.prisma n'est plus
  supporté. La connexion doit passer par prisma.config.ts + un driver adapter.
netsh wlan show profiles permet d'afficher les réseaux wifi enregistrés sur un pc
netsh wlan show profile name="NomDuWifi" key=clear affiche toutes les propriétés du wifi sélectionné (NomDuWifi par exemple) notamment le mot de passe  
        
-npm i -g @openai/codex permet d'installer le CLI de codex (agent ia d'openai qui permet d'interagir depuis le terminal)
-Tip: Run /install-github-app to tag @claude right from your Github issues and PRs
-Tip: Use /permissions to pre-approve and pre-deny bash, edit, and MCP tools
      
-curl -fsSL https://claude.ai/install.cmd -o install.cmd && install.cmd && del install.cmd pour installer claude via le terminal cmd   

-⎿ Tip: Working with HTML/CSS? Install the frontend-design plugin:  /plugin install frontend-design@claude-plugins-official

-BQuerry pour extraire les données et faire des analyses

-Jitsi Meet (gratuit, intégrable    
  en iframe, salle générée par réunion, aucun backend à héberger) — ce qui donne
  une vraie réunion dans l'application — tout en gardant la possibilité de
  coller un lien Meet/Zoom externe.  


 Voilà. Ce que tu dois remplir obligatoirement pour activer les alertes :

  Pour le SMS (Africa's Talking — recommandé pour l'Afrique de l'Ouest) :
  1. SMS_ENABLED=true
  2. AT_API_KEY → clé API depuis https://africastalking.com
  3. AT_USERNAME → ton nom d'utilisateur AT (souvent le nom de ton app)

  Pour WhatsApp (Twilio — plus simple à démarrer) :
   Étapes réelles pour activer les notifications WhatsApp

  1. Compte Twilio — crée un compte sur https://console.twilio.com, récupère
  Account SID et Auth Token (page d'accueil de la console).
  2. Numéro WhatsApp :
    - Pour tester tout de suite : active le WhatsApp Sandbox de Twilio (gratuit,
  numéro +14155238886). Limite importante : chaque destinataire (toi, tes
  admins, tes agents terrain) doit d'abord envoyer le code d'activation ("join
  xxx-xxx") depuis son propre WhatsApp vers ce numéro sandbox — sinon Twilio
  refuse d'envoyer. Ça marche pour valider que le système fonctionne, mais pas
  utilisable en prod avec de vrais clients (ils ne feront jamais cette manip).
    - Pour la vraie prod : il faut demander un numéro WhatsApp Business Sender
  via Twilio (vérification d'entreprise + approbation Meta, ça prend quelques
  jours). Comme les rappels de retard sont des messages initiés par toi (pas une
  réponse à un message du client), Meta exige aussi un template de message
  pré-approuvé pour ce type d'envoi.
  3. Variables d'environnement (Vercel + ton .env local) :
  WA_ENABLED=true
  TWILIO_ACCOUNT_SID=...
  TWILIO_AUTH_TOKEN=...
  TWILIO_WA_FROM=+14155238886   # ou ton numéro approuvé en prod
  DEFAULT_PHONE_PREFIX=228      # 228 Togo, 229 Bénin, 225 CIV
  4. Test manuel avant de compter sur le cron : appelle GET /api/cron/alertes
  toi-même (navigateur ou curl) une fois les variables posées, et regarde les
  logs Vercel pour les lignes [WA] ... (erreurs explicites si un numéro ou une
  variable manque).
  5. Vérifie que les numéros des admins/agents terrain (ceux qui reçoivent
  l'alerte aujourd'hui, pas le client) sont bien enregistrés dans
  Gestionnaire/User et ont rejoint le sandbox si tu testes avec le sandbox.
  6. Une fois validé, le cron Vercel (déjà programmé à 08h00) prend le relais
  automatiquement — rien d'autre à déployer.

  Tu veux que je corrige le bug CRON_SECRET maintenant ?

  Sources:
  - Static Configuration with vercel.json
  (https://vercel.com/docs/project-configuration/vercel-json)
  - Getting started with cron jobs
  (https://vercel.com/docs/cron-jobs/quickstart)
  - How can I use env variables in vercel.json file (if possible)? ·
  vercel/vercel · Discussion #9611
  (https://github.com/vercel/vercel/discussions/9611)
  - Managing Cron Jobs (https://vercel.com/docs/cron-jobs/manage-cron-jobs)
  - Troubleshooting Vercel Cron Jobs | Vercel Knowledge Base
  (https://vercel.com/kb/guide/troubleshooting-vercel-cron-jobs)



  À ne pas oublier :
  - CRON_SECRET → une chaîne aléatoire pour sécuriser les endpoints cron (ex:
  génère avec openssl rand -hex 32)
  - DEFAULT_PHONE_PREFIX → 228 si Togo, 229 si Bénin, 225 si CIV

  Les seuils d'alertes (retard, inactivité, etc.) sont tous optionnels — les
  valeurs par défaut commentées s'appliquent si tu ne les mets pas.

  

L'Inventaire doit se faire chez L'admin et le magasinier (validation inventaire et approvisionnement)

créer un gestionnaire commercial (responsable marketing)

l'admin et les responsables (chefagence , rpv,admin  ) doivent pouvoir modifier et supprimer(uniquement les admins, les autres gestionnaires ne peuvent pas) les factures

erreur d'application et Exception côté client sur quasiment toutes les actions(post ou patch surtout et même get je crois) d'un de mes supérieurs sur l'app déployée (sur vercel) 



Pour les archivages des crédits clients au niveau du dashboard admin/superadmin, je veux le montant total des impayés et des autres status

On va revoir aussi les reçus dans les dashboards

Permettre certaines notifications via mail (vu que maintenant j'ai implémenté l'authification par Google)

Pour les connexion google , demander aussi un autre mot de passe pour empêcher les passant de pouvoir se connecter facilement avec le google d'un utilisateur


Ajouter personne à prévenir ( lien familial et numéro) et la photo dans dashboard/admin/rh/collaborateurs/id


Permettre d'envoyer les documents OGRH ( et tous les autres d'ailleurs) sur les emails


Pour les rh/audit tu es sûr d'avoir fait 


Pour la gestion des commissions et le paramètre des profils de de collaborateur on doit revoir ça


Permettre d'avoir des rapports de recouvrement (taux de recouvrement, restant , payé etc etc ria par mois sur le dashboard admin ria et responsableRIA )


Sur les factures de credit on veut voir aussi les mouvements de compte courant si utilisé ( avance , solde etc etc )

Dans les crédits clients , on va essayer de faire des filtres par agent ( avec leur solde , retard , paiement etc etc)
    

Dans les dossiers des collaborateurs pour leurs informations, faut ajouter aussi personne à prévénir

Pouvoir rajouter grossiste dans les types de fournisseur

réussir les notifs par whatsapp 


Vérifie si ces choses seront bénéfiques et rendront l'app plus puissantes SEO , Embed stream , Cache/ISR, TanStack Query, SSE ou polling . Tu me donnes juste la réponse après analyses poussées et après je te dirai quoi faire

Il reste aussi les sections du CDC non repassées au crible dans
  cette re-vérification (§1-15, §21-23, §27-45 hors §40/§43, §51-56) 


  Euh en fait je remarque que quand j'encaisse un crédit client sur dashboard/admin/credit , le jour sélectionné et le montant sélectionné notammment n'ont aucun impact sur le bordereau de remboursement ( ça ne update pas pourtant logiquement après chaque remboursement , le bordereau automatiquement devrait prendre en compte les jours , les montants collectés et restant  ); et aussi au niveau de dashboard/admin/clients/id , onglet crédits ( ici ça affiche le montant collecté en effet mais le statut du jour de collecte reste sur retard ce qui n'a pas de sens) ; en plus même au moment de collecter un remboursement de crédit client , on peut choisir 2 fois le même jour de collecte . Revois moi tout ça stp , c'est inconcevable ce genre de bugs faciles dans une app qui vise tant l'excellence 

  normalement le comptable doit recevoir les notifications quand le caissier fait ses enregistrements ( entrées , sorties que lui doit confirmer avec pièces justificatives) et même quand les agents font des collectes , quand le rvc ou celui concerné crée des dettes


Au niveau des portails user , pour les présences , est ce qu'on peut déplacer ce bouton flottant pour le mettre à la place des infos ( nom ,prénom ,point de vente qui sont déjà récupérés au niveau du bloc de profil utilisateur à droite) ??