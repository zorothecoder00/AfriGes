-Pages user qu'il me reste à créer( types de gestionnaires [ COMMERCIAL, RESPONSABLE_VENTE_CREDIT, RESPONSABLE_ECONOMIQUE, RESPONSABLE_MARKETING ] )

-Je viens de découvrir Print CSS et window.print() pour les export PDF/Excel
On peut aussi utiliser du SVG pour les graphiques ( sans forcément dépendance externe)  
Ce code :      
 const a = document.createElement("a");
 a.href = url;                  
 a.download = filename;                                       
 a.click();                                                             
 permet de créer un téléchargement via un clic sur une URL ( utile pour les exports)
import { randomUUID } from "crypto";  permet de générer des UUID(sorte d'identifiant unique aléatoire) par exemple pour des factures ou des pièces justificatives
Prisma 7 est une breaking change majeure : url dans schema.prisma n'est plus
  supporté. La connexion doit passer par prisma.config.ts + un driver adapter.
netsh wlan show profiles permet d'afficher les réseaux wifi enregistrés sur un pc
netsh wlan show profile name="NomDuWifi" key=clear affiche toutes les propriétés du wifi sélectionné (NomDuWifi par exemple) notamment le mot de passe

  
-On doit pouvoir aussi affecter les clients aux commerciaux (agents de terrain)

-Fais des await dans toutes mes api où je fais trop de requêtes parallèles



-npm i -g @openai/codex permet d'installer le CLI de codex (agent ia d'openai qui permet d'interagir depuis le terminal)
      
-curl -fsSL https://claude.ai/install.cmd -o install.cmd && install.cmd && del install.cmd pour installer claude via le terminal cmd 

-créer un endpoint léger /api/app-settings accessible à tout
  utilisateur authentifié, et l'appeler au démarrage dans AppSettingsContext
  pour toujours synchroniser DB → localStorage → DOM.
 -Mettre à jour AppSettingsContext pour appeler cet endpoint au
  démarrage et synchroniser les settings depuis la BDD

-Pouvoir customiser les accès en lecture côté admin/superadmin
Architecture:
  Nouveau fichier lib\pagesRegistry.ts   
  Nouveau modèle Prisma RolePageAccess (config par rôle) + réutilisation de
  UserPermission existant (override individuel)
  - API GET/PUT /api/admin/acces-roles + GET/PUT /api/admin/acces-roles/[userId]
  - API GET /api/user/page-access (côté gestionnaire)
  - Hook usePageAccess() partagé
  - Nouvel onglet "Accès" dans la page superadmin
  - Import de usePageAccess from "@/hooks/usePageAccess et Enforcement dans les 7 pages gestionnaire (RPV, Caissier, Magasinier,
  Logistique, Comptable, Chef Agence, Agent Terrain)
  
-Connecter les onglets "Modules et accès" puis "Accès sections":
  Le plan : ajouter un champ module dans chaque section du registre, puis dans
    /api/user/page-access croiser avec les modules actifs — si le module est
    désactivé, la section est bloquée quelles que soient les configs
    rôle/utilisateur.      

-Correction de l'onglet "Utilisateurs" sur le dashboard/admin/superadmin
  1. force_disconnect — purement cosmétique (JWT, aucune vraie invalidation)
  2. suspend — change l'état en DB mais le JWT reste valide 1h
  3. Suppression définitive — un seul clic, aucune confirmation
  4. selectedUser stale — le modal détail garde les vieilles permissions après
  ajout
  5. Impossible de supprimer une permission — remove_permission existe en API
  mais aucun bouton dans l'UI
  6. motifAction pas effacé quand on ferme un modal avec X

  Solutions
  1. ajout de la ligne tokenVersion dans le model User  
  2. ajout de tokenVersion et de error dans l'interface session au niveau de types\next-auth.d.ts
  3. ajouter la lecture du tokenVersion au login, et
  la vérification etat+tokenVersion sur chaque appel suivant dans le fichier lib/authOptions.ts
  4. getAuthSession() — rejeter la session si elle porte une erreur
  5. Maintenant force_disconnect dans l'API action — incrémenter tokenVersion pour
  vraiment révoquer le JWT dans le fichier \api\superadmin\users\[id]\action\route.ts

-Implémentation d'un mécanisme de changement de mot de passe , si l'admin/superadmin réinitialise les mots de passe d'un compte:
  1. Champ mustChangePassword sur User — posé à true lors du reset
  2. API PATCH /api/user/change-password — l'utilisateur change son mot de passe
  3. Middleware/guard — redirige vers une page de changement de mot de passe si
  le flag est actif
  4. Page /auth/change-password — formulaire simple
  5. Création de SessionGuard dans '@/components/SessionGuard pour détecter SESSION_INVALID côté client et forcer la déconnexion puis import dans dashboard/layout

-Donner un accès en lecture à l'admin/superadmin pour tous les dashboards des user/gestionnaires:
  Ce qu'il faudrait créer/modifier
  Fichier: lib/viewAs.ts
  Action: Helper resolveGestionnaire(req, role) — détecte admin+viewAs ou
  session
    propre
  ────────────────────────────────────────
  Fichier: contexts/ViewAsContext.tsx
  Action: Contexte React + cookie
  ────────────────────────────────────────
  Fichier: components/ViewAsBanner.tsx
  Action: Bandeau sticky "Mode lecture — [Nom] ([Rôle])" + bouton sortir
  ────────────────────────────────────────
  Fichier: app/dashboard/layout.tsx  
  Action: Intégrer le banner et le context ViewAsContext
  ────────────────────────────────────────
  Fichier: hooks/useApi.ts
  Action: Injecter ?viewAs=userId automatiquement sur les GETs
  ────────────────────────────────────────
  Fichier: proxy.ts
  Action: Autoriser les admins sur /dashboard/user/* si cookie viewAs présent
          Blocage mutations en mode viewAs pour que l'admin regarde mais ne puisse rien modifier
  ────────────────────────────────────────
  Fichier: app/dashboard/admin/gestionnaires/page.tsx
  Action: Bouton "Voir dashboard" par gestionnaire
  ────────────────────────────────────────
  Fichier: Toutes les routes API gestionnaire
  Action: Remplacer getXxxSession() par resolveGestionnaire()


Faire des tags dans l'application pour permettre de retracer les informations sur les clients


